$('#remote-container').html("<%= j render 'featured_listings/contact_owner_form' %>");

<% unless browser.device.mobile? %>
	$('#frbo-contact-modal').modal('show');
<%else%>
	$('.frbo-contact-modal').slideDown(200);
	$('.closeContact').click(function(e) {
    e.stopPropagation();
    $('.frbo-contact-modal').slideUp(200);
  });
<%end%>
var lm_height = 736;
setTimeout(function(){
	$('.frbo-contact-modal').height(lm_height);	
}, 1000);

setTimeout(function(){
	$.fancybox.close();
}, 100);


