<%= render 'buildings/partials/map_data_field' %>
<div id="mapViewSearch">
  <div class="mapPlaceholder"><span class="fa fa-spin fa-spinner"></span> Loading map...</div>
</div>

<div id="content" class="searched-properties">
  <div class="tabsWidget tabsWidgetSearch">
    <ul class="nav nav-tabs nav-justified" role="tablist">
      <li class="active">
        <a href="#buildings" role="tab" data-bs-toggle="tab">
          <p class="mb-3 font-16 pt-3" style='margin-left: 8px;'>SAVED BUILDINGS <%= "(#{ @buildings.count })" if @buildings.present? %>
        </a>
      </li>
    </ul>
    
    <div class="tab-content tab-scroll11" style="background-color: #f3f3f3;">
      <div class="tab-pane fade in active" id="buildings">
        <%if @buildings.present? %>
          <div class="resultsList">
            <div class="row" id="load-more-buildings">
              <%= render partial: 'buildings/building', :collection => @buildings, :as => :object, cached: true %>
            </div>
          </div>
          
          <br />
          
          <div class="pages-container">
            <%= render 'layouts/shared/add_pagination', objects: @buildings %>
          </div>
        <%end%>
      </div>
    </div>
  </div>
</div>
<div class="clearfix"></div>