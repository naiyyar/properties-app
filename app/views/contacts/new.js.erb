$('#remote-container').html("<%= j render 'contacts/contact_building_modal' %>");
<% unless browser.device.mobile? %>
	$('#newConactModal').modal('show');
<%else%>
	$('.lease-contact-modal').slideToggle(200);
	$('.closeContact').click(function(e) {
    e.stopPropagation();
    $('.lease-contact-modal').slideToggle(200);
  });
<%end%>
var lm_height = $(document).height();
setTimeout(function(){
	$('.lease-contact-modal').height(lm_height);	
}, 1000);


