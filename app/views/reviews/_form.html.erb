<hr/>
<%= form_for [reviewable, Review.new] do |f| %>
  <%= f.text_area :building_review_title,  class: 'form-control' %>
  <%= f.hidden_field :user_id, value: current_user.id %>
    <br />
    <%= f.submit 'Submit', class: "btn btn-primary" %>
<% end %>
  