$('#remote-container').html('<%= j render partial: "featured_agents/contact_agent_modal" %>');

<% unless browser.device.mobile? %>
	$('#contactAgentModal').modal('show');
<%else%>
	var elem = $('.contact-agent-modal');
	elem.slideDown(200);
	$('.closeContact').click(function(e) {
    e.stopPropagation();
    elem.slideUp(200);
  });
<%end%>

// var lm_height = 736;
// setTimeout(function(){
// 	$('.contact-agent-modal').height(lm_height);	
// }, 1000);

$('.multiselect').multiselect();