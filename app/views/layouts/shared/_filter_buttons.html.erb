<div class="visible-xs filter-btn-container">
	<!-- <div class="pull-right btn-hoods hidden">
 		<button data-toggle="dropdown" class="btn btn-green dropdown-toggle">
    <span class="dropdown-label"><%#= params[:neighborhoods].present? ? params[:neighborhoods] : 'Neighborhoods'  %></span> &nbsp;<span class="caret"></span>
    </button>
  </div> -->
  <% if show_filters? %>
	  <div class="btn-filter">
	 		<button class="btn btn-o btn-primary btn-filter handleFilter">
	      <span class="dropdown-label"><span class="fa fa-filter"></span> &nbsp;<%= 'Filter' %> <span class="filter-counts"></span> &nbsp; </span>
	    </button>
	  </div>

	  <div class="btn-filter">
	    <%= link_to "Guides (#{@neighborhood_links.count})", 'javascript:;', class: "btn btn-o btn-primary btn-filter handleGuides", style: 'width: 100%;' %>
	  </div>
		
		<!-- <div class="pull-right btn-sort hidden"> -->
			<!-- <label class="select select-blue" style="margin-bottom: 0px;"> -->
				<%#= select_tag :sort, options_for_select(sort_options, params[:sort_by]), { prompt: 'Sort', data: {search_term: params[:search_term], neighborhoods: params[:neighborhoods] } } %> 
				<!-- <i></i> -->
				<%#= link_to "Guides (#{@neighborhood_links.count})", neighborhood_links_path, remote: true %>
			<!-- </label> -->
		 	<!-- <button data-toggle="dropdown" class="btn btn-o btn-gray dropdown-toggle">
	      <span class="dropdown-label"><span class="fa fa-sort"></span> &nbsp;<%#= params[:sort_by].present? ? sort_string : 'Sort'  %></span> &nbsp; 
	    </button> -->
	    <%#= render partial: 'layouts/shared/sort_list' %>
	  <!-- </div> -->
	<%end%>
</div>
