<nav class="navbar navbar-default">
  <a href="<%= root_path %>" class="logo pull-left">
    <%= image_tag 'logo-o.png' %>
  </a>
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed pull-left" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
<!--       <form class="navbar-form navbar-left" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
      </form> -->
      <ul class="nav navbar-nav">
        <li><%= link_to "Buildings", buildings_path %></li>
        <li><%= link_to "Units", units_path %></li>
        <li><%= link_to "Reviews", reviews_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li class="dropdown", id="user-image">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            <% if current_user.avatar.present? %>
              <%= image_tag current_user.avatar %>
            <% elsif current_user.image_url.present? %>
              <%= image_tag current_user.image_url %>
            <%else%>
              <span class="fa fa-user fa-2x"></span>
            <%end%>
            </a>
            
            <ul class="dropdown-menu">
              <li><a href="#">Settings</a></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to "Sign out", destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li> 
            <a href="javascript:;" data-toggle="modal" data-target="#loginModal" id="signin"> Sign in</a>
          </li>
          <li> 
            <%= link_to '<span class="pipe">|</span>'.html_safe, '#'%>
          </li>
          <li> <a href="javascript:;" data-toggle="modal" data-target="#loginModal" id="register"> Register</a></li>
        <% end %> 
        <li> 
          <%= link_to '<span class="pipe">|</span>'.html_safe, '#'%>
        </li>
        <li>
          <!-- Todo: will redirect to building or unit review page  -->
          <%= link_to '<span class="fa fa-plus"></span>'.html_safe, contribute_buildings_path, class: "btn btn-sm btn-primary btn-plus"%>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>