<% flash.each do |key, value| %>
	<div class="text-center <%= flash_class(key) %> fade in">
		<a href="javascript:void(0)" class="close" data-dismiss="alert" aria-label="close">&times;</a>
	  <span><%= value %></span>
	</div>
<% end %>