<%= content_for :html_title, @tag.name %>

<!-- Category name -->
<h2><%= @tag.name %></h2>

<!-- Recent posts -->
<!-- <h4>Posts</h4> -->

<% @tag.recent_posts.each_with_index do |post, index| %>
	<% @count = index %>
  <%= render :partial => 'buttercms/posts/post', :locals => {:post => post} %>
<% end %>