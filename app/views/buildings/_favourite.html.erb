<div class="pull-right" style="font-size: 22px;">
  <%= heart_link(building, current_user) if user_signed_in? %>
  <%= content_tag 'div', heart_icon, class: "save_link_#{building.id} favourite #{saved_color_class(building)}", onclick: "openSuModal(event,#{building.id});" if !user_signed_in?%>
  
  <%#= link_to heart_icon, 'javascript:;', class: "save_link_#{building.id} favourite #{saved_color_class(building)}", onclick: "openSuModal(event,#{building.id});", 'area-label' => "Save #{building.property_name_or_address} as favourite"  %>
</div>