- if current_user&.has_role? :admin
  - if current_user.present?
    .row
      .col-xs-12.col-sm-12.col-md-12.col-lg-12
        .pull-right= link_to 'Add New Featured Comp', new_featured_comp_path, class: 'btn btn-blue'
    
    %h2
  
    .pull-right= render 'layouts/shared/filterrific_form'
  
  = render 'featured_comps/comps'
  
  .text-center= will_paginate @featured_comps, renderer: BootstrapPagination::Rails

:css
  #sortable_filter{
    display: none;
  }