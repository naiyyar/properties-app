%div{style: 'padding: 10px;'}
	- VideoTour::CATEGORIES.each_with_index do |category, index|
		- video_tours = @video_tours[category[1]]
		- if video_tours.present?
			%p.font-14.font-bold
				=category[0]
			.row
				- video_tours.each do |video|
					.col-lg-4.image-gallery
						%p.font-14=video.try(:description)
						%a.propWidget-1{'data-caption' => video.try(:description), 'data-fancybox' => 'gallery', href: "#{video.url}", 'data-type' => 'iframe'}
							%div{style: 'position:absolute;z-index:500;height:100px;width:100%;'}
							%iframe{src: video.url, style: 'width: 100%;height: 100px;border:none;'}