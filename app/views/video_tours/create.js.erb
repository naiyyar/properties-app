var cat 				= "<%=@video_tour.building_id%>-<%=@category%>_<%=@video_tour.sort%>";
var iframe_elem = $('#video-preview-'+cat);
iframe_elem.attr('src', '<%=@video_tour.url%>');

$('#remove-fields-'+cat).html("<%= j render partial: 'delete_link', locals: {video_tour: @video_tour }%>");

<%if @errors.present? %>
	error_message = "<%=@errors.messages[:url][0]%>"
	bootbox.alert('<b>'+error_message+'</b>');
<%end%>