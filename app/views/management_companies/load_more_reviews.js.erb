var total_reviews = <%=@reviews.count%>;
<% if @reviews.present? %>
	$('.objects-container').append('<%=escape_javascript(render partial: @reviews) %>');
	if(total_reviews < 10){
		$('a.load-more').hide();
	}
<%end%>

load_rating_stars();
